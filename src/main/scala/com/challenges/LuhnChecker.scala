package com.challenges

/** LuhnChecker is responsible to determine whether a given string is a valid credit card number.
 * 
 */
object LuhnChecker {
  
  def isValid(s: String): Boolean = { 
    return true
  }
}